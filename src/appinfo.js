// export let arr = 5
// setTimeout(() => {
//     arr = 100
//     console.log(arr,'ll');
// }, 3000)
// let arr = [3, 6, 9];

// setTimeout(() => {
//   arr = [88, 99, 10];
//   console.log(arr, 'll');
// }, 0);

// export { arr };

// 只能在当前页面，模块的改变才生效，如果切换路由，切换页面，模块就会重新加载，
// 不能跨页面，不管什么数据类型，什么导出形式，都不能夸页面。注意export default arr 与 export default {arr} 这种区别
